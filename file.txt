UU3cQHaaXTYrRG9z
Suppose React eventually sends:
The request travels:
React
  ↓
POST /api/applications
  ↓
  //The route decides which controller function should run.
Route
  ↓
  //The controller contains the actual application logic.
Controller    //The model talks to MongoDB.
  ↓
Application.create()
  ↓
MongoDB

200 → successful request
201 → resource created
400 → bad request
404 → resource not found
500 → server error

routes
  = Which endpoint?

controllers
  = What should happen?

models
  = What does the data look like?

database
  = Where is the data stored?

  REGISTER
User
 ↓
POST /api/auth/register
 ↓
bcrypt hashes password
 ↓
MongoDB


LOGIN
Email + password
 ↓
POST /api/auth/login
 ↓
bcrypt checks password
 ↓
JWT generated
 ↓
Frontend receives token
Then for protected requests://
React
 ↓
JWT
 ↓
Express
 ↓
Auth Middleware
 ↓
req.user
 ↓
Controller
 ↓
MongoDB
=======

password
   ↓
bcrypt
   ↓
hashed password
   ↓
MongoDB

---------
email
password
   ↓
bcrypt.compare()
   ↓
correct?
   ↓
JWT created


//frontend
pages       → complete screens
components  → reusable UI
services    → backend/API communication
App.jsx     → routing

////http://localhost:5173/
        ↓
      Login

http://localhost:5173/login
        ↓
      Login

http://localhost:5173/register
        ↓
      Register

http://localhost:5173/dashboard
        ↓
      Dashboard

http://localhost:5173/applications/add
        ↓
      Add Application

http://localhost:5173/applications/edit/123
        ↓
      Edit Application